import React from "react";

const page = () => {
  return (
    <div>
      user details (achievements, token balance, contents consumed, created
      contents )
    </div>
  );
};

export default page;
